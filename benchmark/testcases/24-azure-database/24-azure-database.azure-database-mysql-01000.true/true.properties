# This configuration file contains a matching pattern for the rule "azure-database-mysql-01000"

# A JDBC MySQL connection string, which matches the condition "builtin.filecontent" with pattern "(?i)jdbc:mysql"
spring.datasource.url=jdbc:mysql://localhost:3306/mydb

# A MySQL database configuration, which matches the condition "builtin.filecontent" with filePattern ending in ".properties"
mysql.database.username=root
mysql.database.password=mysecretpassword