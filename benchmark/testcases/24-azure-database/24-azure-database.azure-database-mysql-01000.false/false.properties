# This configuration file DOES NOT contain any pattern matching the rule "azure-database-mysql-01000"

# A typical unrelated configuration example with no reference to MySQL or any matching pattern
spring.datasource.url=jdbc:postgresql://localhost:5432/mydb

# Another database configuration that does not fit the rule criteria
postgres.database.username=admin
postgres.database.password=securepassword